# Runtime dependencies for Lucid.
#
# This file installs everything needed to train, evaluate and export. For a
# minimal set, `pip install -e .` installs only the core; the extras below are
# declared in pyproject.toml and can be installed selectively, e.g.
#   pip install -e ".[tracking,export]"
#
# Development tools (pytest, ruff, pre-commit) are NOT listed here -- they live
# in the `dev` extra, so a production image does not carry a test runner.
# Install them with:  pip install -e ".[dev]"

# -- Core ---------------------------------------------------------------------
torch>=2.0
numpy>=1.24
h5py>=3.8
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7

# -- Experiment tracking ------------------------------------------------------
tensorboard>=2.14
wandb>=0.15

# -- Deployment / export ------------------------------------------------------
onnx>=1.14
onnxruntime>=1.16

# -- Optional: VGG perceptual loss (training.loss.perceptual_weight > 0) ------
torchvision>=0.15
